<% content_for(:title, @post.title) %>
<div class="main_image py-10">
  <div class="row">
    <h1 class="text-3xl items-center justify-center text-center pb-10"><%= t '.title' %></h1>
    <%= render 'form', { post: @post } %>
  </div>
</div>
